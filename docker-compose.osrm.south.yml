services:
  osrm-south:
    image: osrm/osrm-backend:latest
    container_name: trek_osrm_south
    restart: unless-stopped
    ports:
      - "5001:5000"
    volumes:
      - ./osrm-data:/data
    command: osrm-routed --algorithm mld -t 1 /data/southern-zone-latest.osrm
